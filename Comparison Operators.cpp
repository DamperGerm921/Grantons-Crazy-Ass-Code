#include <iostream>

using namespace std;

int main() {
    int x = 10;
    double y = 5;
    bool result = x == y;
    cout << boolalpha << result << endl;

    //If I were to compare letters, it would be CASE SENSITIVE
    // As with all things in C++

    return 0;
}
